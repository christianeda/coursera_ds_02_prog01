touch README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/christianeda/testing_last.git
git push -u origin master
#…or push an existing repository from the command line

#vom remote template noch lokal klonen
git clone https://github.com/christianeda/analytics_template.git xxxxxxx

git status
it remote -v

#origin entfernen
git remote rm origin


OPEN existing directory 'xxxxxxx' in Rstudio

(git push -u origin master)

#nachsehen
git remote -v
git branch -a







